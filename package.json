{
  "name": "eventora",
  "private": true,
  "scripts": {
    "build:client": "npm --prefix client ci && npm --prefix client run build",
    "build:server": "npm --prefix server ci",
    "build": "npm run build:client && npm run build:server",
    "start": "npm --prefix server start"
  },
  "engines": {
    "node": ">=18"
  }
}
